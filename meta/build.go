package meta

// This package is the
var (
	// BuildDate contains a string with the build date.
	BuildDate = "unknown"
	// BuildCommitHash contains a string with the git commit hash.
	BuildCommitHash = "unknown"
	// BuildReleaseVersion contains a string with the compiled release version.
	BuildReleaseVersion = "dev"
)
